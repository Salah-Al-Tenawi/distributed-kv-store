// =============================================================
// states.js — الحالات الممكنة لأي عقدة
// =============================================================
// في خوارزمية Raft كل عقدة تكون في إحدى ثلاث حالات.
// نعرّفها هنا ككائن ثابت (مثل enum) لنتجنّب الأخطاء الإملائية.

const NodeState = {
  FOLLOWER: 'FOLLOWER',     // تابع: ينتظر أوامر القائد (الوضع الافتراضي)
  CANDIDATE: 'CANDIDATE',   // مرشّح: يطلب أصوات ليصبح قائداً
  LEADER: 'LEADER',         // قائد: يدير الكتابة ويرسل النبضات
};

module.exports = { NodeState };
